export interface IBookingDetails {
    from: string;
    to: string;
    journeyDate: string;
    journeyQuota: string;
  }
  
  export interface IStation {
    stationName: string;
    stationCode: string;
  }